# Copy to .env and fill in values before running docker-compose

# Required: shared secret between pipeline service and backend
PIPELINE_API_KEY=change-me-to-a-random-uuid

# Optional overrides (defaults shown)
MAX_WORKERS=1
JOB_TTL_SECONDS=300
MAX_IMAGE_BYTES=10485760
